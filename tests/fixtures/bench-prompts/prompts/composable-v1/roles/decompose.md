Its `description` must begin with the
actual concrete task. Omit it entirely when it
would be boilerplate. This is guidance, not an Orboros-executed contract.
Do not create an inspection-only child. do not require
  a later-owner label or a detailed handoff plan. when they edit the same file,
use an order. focused scope is guidance, not an artificial
ceiling.
